{
  "name": "rust-nix-hm",
  "build": {
    "dockerfile": "../devcontainer.dockerfile",
    "context": ".."
  },
  "remoteUser": "vscode",
  // "overrideCommand": false,
  // "postCreateCommand": "home-manager switch -b backup --flake /workspaces/${localWorkspaceFolderBasename}/.devcontainer#devcontainer || home-manager switch -b backup -f /workspaces/${localWorkspaceFolderBasename}/home.nix",
  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "jnoortheen.nix-ide"
      ]
    }
  }
}